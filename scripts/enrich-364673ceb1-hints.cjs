#!/usr/bin/env node
// Enrich token.hint for kobun-tan 教材「車争ひ」(364673ceb1)
// 源氏物語 葵巻 賀茂祭御禊での葵の上と六条御息所の車争いの場面
// Strategy: per-token rule based on text + grammarTag (pos/baseForm/meaning/conjugationForm)

const fs = require('fs');
const path = require('path');

const SRC = path.resolve(__dirname, '../public/texts-v3/364673ceb1.json');
const data = JSON.parse(fs.readFileSync(SRC, 'utf8'));

function buildHint(tk) {
  if (tk.hint) return null;
  const g = tk.grammarTag || {};
  const text = tk.text;
  const pos = g.pos || '';
  const base = g.baseForm || '';
  const meaning = g.meaning || '';
  const form = g.conjugationForm || '';
  const ctype = g.conjugationType || '';

  // ===== 車争ひ特有の重要語（最優先）=====
  // これらは grammarTag の pos が空でも text マッチで先にカバー
  if (text === '網代' || text.includes('網代')) {
    return '名詞「網代車（あじろぐるま）」: 檜の薄板を網目に編んだ車体の牛車。本来は中流以下の身分が用いる目立たない車で、「やつし」（身を粗末に装う）に好適';
  }
  if (text === '下簾') {
    return '名詞「下簾（したすだれ）」: 牛車の前後の簾の内側にかける薄絹の布。貴人の車内を外から見えなくする目隠し。袖口や裾を出して色目で乗り手の品格をほのめかす';
  }
  if (text === '汗衫') {
    return '名詞「汗衫（かざみ）」: 童女・若い女性が肌着の上に着る薄絹の汗取り衣。下簾から覗かせる袖口・裾の襲色目（かさねのいろめ）が乗り手の身分・センスを示す';
  }
  if (text === '榻') {
    return '名詞「榻（しぢ）」: 牛車の轅（ながえ）の先を載せて車を水平に支える木製の台。駐車中の車の格を保つ必需品で、これを折ることは貴人の体面を踏みにじる象徴的暴行';
  }
  if (text === '轅' || text.includes('轅')) {
    return '名詞「轅（ながえ）」: 牛車の前に長く突き出た二本の長柄。牛をつないで引かせる中心部材。先端を榻に載せて駐車する';
  }
  if (text === '筒') {
    return '名詞「筒」: 他人の牛車の轅受け（轅の根元を受ける部分）を指す。榻を折られて、見ず知らずの車の筒に轅を引っ掛ける = 身分ある女主人にとって極めて屈辱的な状態';
  }
  if (text === '副車' || text.includes('副車')) {
    return '名詞「副車（ひとだまひ・そえぐるま）」: 主人の本車に従うお供の車。本来主車が前で物見できるべき位置から押しやられて副車の奥に追いやられるのは、人前での明白な敗北・屈辱';
  }
  if (text === '車副' || text.includes('車副')) {
    return '名詞「車副（くるまぞい）」: 牛車の左右に付き従う徒歩の従者。場所取り・道明けなど物理的なトラブルの実行役で、この場面の暴力もこの車副たちが行う';
  }
  if (text === '物見車' || text === '物見') {
    return '名詞「物見車（ものみぐるま）」: 祭礼・行列の見物のために大路に止める牛車。賀茂祭の御禊では一条大路に何重にも並び、よい場所を取れるかが当日の楽しみと体面を左右した';
  }
  if (text === '斎宮' || text.includes('斎宮')) {
    return '名詞「斎宮（いつきのみや・さいぐう）」: 伊勢神宮に奉仕する未婚の内親王・女王。卜定（ぼくじょう）で選ばれ、潔斎ののち伊勢へ下る。母・六条御息所は娘に付き添い伊勢へ行こうとしている';
  }
  if (text === '御息所' || text.includes('御息所')) {
    return '名詞「御息所（みやすんどころ）」: 皇太子・親王の妃の称号。六条御息所は亡き前東宮（先帝の弟皇太子）の妃で、源氏の母方にも近い最高級の貴族女性';
  }
  if (text === '大将殿' || text.includes('大将殿')) {
    return '名詞「大将殿」: 近衛大将の地位にある光源氏の呼称。葵の上方の供人が御息所方を侮蔑して「大将殿の威光を頼りにしているのだろう」と皮肉る場面で用いる';
  }
  if (text === '大殿') {
    return '名詞「大殿（おほとの）」: ここでは葵の上の実家・左大臣家のこと。牛車の標識（紋・轅の太さ）で一目でわかる正妻の車列';
  }
  if (text === '御前' || text === '御前渡り') {
    return '名詞「御前渡り（おまへわたり）」: 貴人がある場所の前を通り過ぎることへの敬称。ここでは源氏が御息所の車の前を通る瞬間を待つ御息所の心情を示す';
  }
  if (text === '御方') {
    return '名詞「その御方（みかた）の人」: 源氏に仕える従者。葵の上方にも御息所方にも混じっており、両者の関係を知るゆえに同情するが、仲裁は避ける';
  }
  if (text === '豪家') {
    return '重要語「豪家（がうけ）」: 権勢ある家・有力な後ろ盾。「大将殿をぞ豪家には思ひきこゆらむ」=「源氏の威光を頼みにしているのだろう」と御息所を見下す皮肉';
  }
  if (text === '笹' || text === '笹の隈') {
    return '名詞「笹の隈（ささのくま）」: 古今集「笹の隈檜の隈川に駒とめてしばし水かへ影をだに見む」を引く引き歌。「駒を止める場所でさえないから」=御息所を素通りする源氏の冷淡さを古歌に重ねる';
  }
  if (text === 'みたらし川' || text.includes('みたらし')) {
    return '名詞「みたらし川（御手洗川）」: 御禊の場で身を清める川。御息所の歌では「身（み）」の音を掛け、源氏に冷たくされる「我が身の憂さ」を重ねる縁語の核';
  }
  if (text === '影') {
    return '重要語「影」: 姿・面影・水面の影の三義。「影をのみみたらし川の…」では御息所が源氏の姿を遠くから見るだけに留まる屈辱を、川面の「影」に重ねる';
  }
  if (text === '出でばえ') {
    return '名詞「出でばえ」: 晴れの場で一段と映え立つこと。源氏が行列の主役として人々の目の前に登場する華やかさ。御息所の苦しみと対比される';
  }
  if (text === '容貌') {
    return '名詞「容貌（かたち）」: 顔立ち・姿。「目もあやなる御さま・容貌」=まぶしいほど美しい源氏の容姿。御息所の執着の根源';
  }
  if (text === '物の色') {
    return '名詞「物の色」: 衣類の襲色目（かさねのいろめ）。下簾の隙間から見える袖口・裳裾・汗衫の色合いが乗り手の身分・教養・センスを示す視覚記号';
  }
  if (text === 'やつれ' || text === 'やつる') {
    return '重要語「やつる」: お忍びの粗末な姿をする・目立たないように装う。前東宮妃の御息所が網代車に乗るのは「やつし」の作法だが、品の良さは隠せない';
  }
  if (text.startsWith('つれなしづくる') || text.startsWith('つれなしづくれ')) {
    return '重要語「つれなしづくる」: そしらぬふりをする・平静を装う。動揺を抑えて気づかれまいとする御息所の心理。直後の「おのづから見知りぬ」と対比される';
  }
  if (text.startsWith('まめだち') || text === 'まめだち') {
    return '重要語「まめだつ」: まじめくさる・きちんとした態度をとる。源氏が葵の上方には会釈する様子を示し、御息所への素通りと対比して屈辱を決定づける';
  }
  if (text === 'よそほしう' || text.startsWith('よそほし')) {
    return '重要語「よそほし」: いかめしい・きちんとしている・威儀を整えている。表向き「わざとならず」(気軽な装い) を装っても実は堂々たる正妻の貫禄が滲む';
  }
  if (text === 'よしばめ' || text.startsWith('よしばめ')) {
    return '重要語「よしばむ」: 趣向を凝らして上品ぶる・由緒ありげに見せる。下簾の様子に風情を凝らした車の様子。お忍びの中にも品位が滲む';
  }
  if (text === 'おとなおとなしき' || text.startsWith('おとなおとなし')) {
    return '重要語「おとなおとなし」: 年配で分別がある・大人びている。若い供人と対比され、年配の御前駆の人々が制止しようとする立場を示す';
  }
  if (text.startsWith('心やまし') || text === '心やましき') {
    return '重要語「心やまし」: 心が痛む・不愉快だ・しゃくにさわる。御息所が物理的屈辱（榻折り）と精神的屈辱（身元発覚）の両方に苦しむ感情の核';
  }
  if (text === 'ねたき' || text.startsWith('ねた')) {
    return '重要語「ねたし」: しゃくにさわる・悔しい・腹立たしい。現代語「妬ましい」とは語感が異なり、自尊心を傷つけられた屈辱・不快の中心語';
  }
  if (text === 'すずろなる' || text.startsWith('すずろ')) {
    return '重要語「すずろなり」: わけもなく・無関係だ・思いがけない。「すずろなる車」=見ず知らずの他人の車。轅を引っ掛けられる相手としての無縁さが屈辱を増幅';
  }
  if (text === 'またなう' || text.startsWith('またな')) {
    return '重要語「またなし」: 二つとない・並ぶものがない。ここは否定的に「これ以上ないほど（人わろし）」=最高に体裁が悪い、の意。最上級表現';
  }
  if (text === '人わろく' || text.startsWith('人わろ')) {
    return '重要語「人わろし」: 体裁が悪い・見苦しい・人聞きが悪い。他人の目を気にする貴族の体面意識の核となる語';
  }
  if (text === '悔しう' || text.startsWith('悔し')) {
    return '重要語「悔し」: 後悔する・残念だ。「何しに来つらむ」と自責する御息所の絶望を端的に示す';
  }
  if (text === 'かひなし' || text.startsWith('かひな')) {
    return '重要語「かひなし」: どうしようもない・無駄だ・甲斐がない。「思ふにかひなし」=思い悩んでも何にもならない、の手遅れ感';
  }
  if (text === 'いとほし' || text.startsWith('いとほし')) {
    return '重要古今異義語「いとほし」: 気の毒だ・かわいそうだ。現代語「愛しい」とは違う。「お気の毒」が古文の基本義';
  }
  if (text === 'わづらはしけれ' || text.startsWith('わづらはし')) {
    return '重要語「わづらはし」: 厄介だ・面倒だ・気がかりだ。仲裁すれば自分にも累が及ぶ厄介事への忌避心';
  }
  if (text === 'つれなき' || text === 'つれなく' || text.startsWith('つれな')) {
    return '重要語「つれなし」: そっけない・冷淡だ・素知らぬ顔だ。源氏が御息所の車を素通りする冷淡さの核語。歌では川面の「冷たさ」と二重写し';
  }
  if (text === '心づくし' || text.includes('心づくし')) {
    return '重要語「心づくし」: 心を尽くすほどあれこれ思い悩む・気苦労。「なかなか御心づくしなり」=かえって思い悩むことが増す、の倒錯';
  }
  if (text === 'なかなか') {
    return '重要副詞「なかなか」: かえって・むしろ・中途半端に。「なかなか〜なり」で予想と逆の結果を強調。素通りされたことが「いっそ目に入らなければよかった」と苦しみを増幅';
  }
  if (text === 'はしたなけれ' || text.startsWith('はしたな')) {
    return '重要古今異義語「はしたなし」: きまりが悪い・中途半端で居心地が悪い。現代語「はしたない」(下品) と意味が違う。涙を見られる気まずさを示す';
  }
  if (text === 'あやなる' || text.startsWith('あやな')) {
    return '重要語「あやなし」: わけがわからない・道理に合わない。「目もあやなる」=まぶしくて目を開けていられないほど美しい、の慣用句';
  }
  if (text === 'いとどしう' || text.startsWith('いとどし')) {
    return '重要語「いとどし」: いっそう増す・ますます強い。「いとど」(副詞) の形容詞化。状態の累加・進行を表す';
  }
  if (text === '口強く' || text.startsWith('口強')) {
    return '重要語「口強し」: 言葉が強い・強情に言い張る・言いかえす。御息所側の供人が葵の上方の威勢に対抗する気概を示す';
  }
  if (text === 'しるく' || text === 'しるけれ' || text.startsWith('しるし')) {
    return '重要語「しるし（著し）」: はっきりしている・明瞭だ・目立つ。「ことさらにやつれたるけはひしるく見ゆる」=お忍び姿がかえってくっきり目立つ逆説';
  }
  if (text === 'いみじう' || text.startsWith('いみじ')) {
    return '重要語「いみじ」: 程度が甚だしい (良いにも悪いにも)・たいそう・ひどい。文脈で「素晴らしい」「ひどい」を見分ける。ここは負の意で「ひどく悔しい」';
  }
  if (text === 'こよなう' || text === 'こよなし' || text.startsWith('こよな')) {
    return '重要語「こよなし」: この上ない・格段だ・比べようもない。「こよなう思さる」=この上なく（圧倒されたと）感じられる';
  }
  if (text === 'いたう' || text.startsWith('いた')) {
    return '副詞「いたう」: ひどく・たいそう・はなはだしく。「いたく」のウ音便。打消を伴うと「あまり〜ない」(部分否定)';
  }
  if (text === 'ほのかなる' || text.startsWith('ほのか')) {
    return '形容動詞「ほのかなり」: かすかだ・わずかに見える・ぼんやりしている。下簾の隙間からチラリと覗く袖口・裳裾の様子';
  }
  if (text === 'ことさらに' || text.startsWith('ことさら')) {
    return '副詞「ことさらに」: わざわざ・とりわけ・故意に。「ことさらにやつれたる」=わざと粗末に装ったお忍びの姿';
  }
  if (text === 'さやうに' || text.startsWith('さやう')) {
    return '形容動詞「さやうなり」: そのようだ・そのように。「さやうにさし退け」=そんなふうに立ち退かせる';
  }
  if (text === 'きよらに' || text.startsWith('きよら')) {
    return '重要語「きよらなり」: 気高く美しい・清らかで美しい。最上級の美の表現。下簾から覗く色目の上品さ';
  }
  if (text === 'なき' || text === 'なく' || text === 'なう' || text === 'なけれ') {
    return '形容詞「なし」 (' + form + '形): 「無し」。ク活用。連体形「なき」連用形「なく」(ウ音便「なう」) 已然形「なけれ」が頻出';
  }
  if (text === 'よき') {
    return '形容詞「よし」: 良い・身分が高い・上品だ。「よき女房車」=身分ある女性の乗った立派な車';
  }
  if (text === '若き') {
    return '形容詞「若し」: 若い。「若き者ども」=年配の御前駆と対比される下位の若い供人たち';
  }
  if (text === '多く') {
    return '形容詞「多し」: 数が多い。「よき女房車多くて」=立派な女房車が多くて隙間がない';
  }
  if (text === '少し') {
    return '副詞「少し」: わずかに・ちょっと。「少し馴れたる」=少し使い込まれた様子の網代車';
  }
  if (text === '皆') {
    return '副詞「皆（みな）」: 全部・すべて。「皆さし退けさする」=（雑々の人の車を）全部立ち退かせる、葵の上方の暴力性を強調';
  }
  if (text === 'みな') {
    return '副詞「みな」: 全部・すべて。「榻などもみな押し折られて」=榻なども全部折られて、徹底した破壊の様子';
  }
  if (text === 'つひに') {
    return '副詞「つひに」: とうとう・最終的に。「つひに御車ども立てつづけつれば」=とうとう（葵の上方の）車を並べてしまったので、抵抗の限界を示す';
  }
  if (text === 'さらに') {
    return '副詞「さらに」: 改めて・決して (打消と呼応)。「さらに〜ず」=決して〜ない、の強い打消呼応で頻出';
  }
  if (text === 'おのづから') {
    return '重要副詞「おのづから」: 自然と・ひとりでに・たまたま。意志を介さない発生・偶然性。「おのづから見知りぬ」=（隠そうとしても）自然に見抜かれた';
  }
  if (text === 'げに') {
    return '重要副詞「げに」: 本当に・なるほど。語り手の同意・納得の合いの手で読者を引き込む';
  }
  if (text === 'さばかり') {
    return '副詞「さばかり」: その程度・あの程度。「さばかりにては」=「あの程度の身分では」と御息所を見下す侮蔑表現';
  }
  if (text === 'いとど') {
    return '重要副詞「いとど」: いっそう・ますます。「いと」の強調形。状態の累加・進行を強める';
  }
  if (text === 'え' && pos === '副詞') {
    return '重要副詞「え」: 下に打消・反語を伴って「〜できない」(不可能)。「え〜ず／え〜じ」呼応で頻出';
  }
  if (text === 'かく') {
    return '指示副詞「かく」: このように・こう。「かくな」=「こう（騒ぎ立てる）な」と年配者が若者を制止する発話';
  }
  if (text === 'さ') {
    return '指示副詞「さ」: そのように・そう。「さな言はせそ」=そんなことを言わせるな';
  }
  if (text === 'わざと') {
    return '副詞「わざと」: ことさら・特別に・本格的に。「わざとならぬさま」=ことさら儀式張らない様子（実は正妻の威厳ある外出）';
  }
  if (text === 'いと' && pos === '副詞') {
    return '副詞「いと」: 非常に・たいそう。古文最頻出の程度副詞。打消を伴うと「あまり〜ない」(部分否定)';
  }

  // ===== 助動詞 =====
  if (pos === '助動詞') {
    if (text === 'けり') return '助動詞「けり」: 過去 (〜た) ＋ 詠嘆 (〜だなあ)。連用形接続。「出でたまへるなりけり」では語り手が今気づいて読者に開示する発見の含み';
    if (text === 'なり' && meaning === '断定') return '助動詞「なり」: 断定 (〜である)。体言・連体形に接続。「なりけり」で語り手の発見を伴う説明的な余情';
    if (text === 'なら' && meaning === '断定') return '助動詞「なり」未然形「なら」: 断定 (〜である)。「ならぬ」=〜でない。「わざとならぬさま」=ことさらめかしくない様子';
    if (text === 'なれ' && meaning === '断定') return '助動詞「なり」已然形「なれ」: 断定 (〜である)。「〜なれど」=〜であるけれども';
    if (text === 'に' && meaning === '断定') return '助動詞「なり」連用形「に」: 断定 (〜で)。「にもあらず」「にや」「にて」の核で、後ろに「あり」を補って読む';
    if (text === 'ぬ' && meaning === '打消') return '助動詞「ず」連体形「ぬ」: 打消 (〜ない)。未然形接続。「わざとならぬ」=ことさらめかしくない';
    if (text === 'ね' && meaning === '打消') return '助動詞「ず」已然形「ね」: 打消 (〜ない)。「ねばにや」=「（〜では）ないからであろうか」と語り手の推量';
    if (text === 'ず' && meaning === '打消') return '助動詞「ず」: 打消 (〜ない)。未然形接続。「え〜ず」「さらに〜ず」の呼応で強い不可能・強い打消を作る';
    if (text === 'ぬ' && meaning === '完了') return '助動詞「ぬ」終止形: 完了 (〜た・〜てしまった)。連用形接続。「見知りぬ」=見抜いた、「事なりぬ」=（行列が）始まった';
    if (text === 'ぬる') return '助動詞「ぬ」連体形「ぬる」: 完了 (〜た・〜てしまった)。「知られぬる」=（身元を）知られてしまった、取り返しのつかなさ';
    if (text === 'つ' && meaning === '強意') return '助動詞「つ」: 強意 (きっと〜)。「つらむ」「つべし」など推量と組んで強い予測。「来つらむ」=来てしまったのだろう';
    if (text === 'つれ') return '助動詞「つ」已然形「つれ」: 完了 (〜てしまった)。「立てつづけつれば」=立て並べてしまったので、御息所側の物理的劣勢を確定';
    if (text === 'たり' || text === 'たる' || text === 'たれ') {
      return '助動詞「たり」 (' + form + '形): 完了 (〜た) ／ 存続 (〜ている)。連用形接続。動作の完了点・継続を含意';
    }
    if (text === 'り') return '助動詞「り」: 存続 (〜ている) ／ 完了 (〜た)。サ変未然形・四段已然形に接続。「出でたまへり」=お出かけになっている';
    if (text === 'る' && (meaning === '存続' || meaning === '完了')) return '助動詞「り」連体形「る」: 存続 (〜ている) ／ 完了 (〜た)。「出でたまへるなりけり」=お出かけになっていたのであった';
    if (text === 'る' && meaning === '自発') return '助動詞「る」自発: 自然と〜される (意志に反して)。「思さる」「知らるる」など、御息所の感情描写に頻用される定型';
    if (text === 'るる' && meaning === '自発') return '助動詞「る」連体形「るる」: 自発 (自然と〜される)。「待たるる」=待つまいと思っても自然に待ってしまう、御息所の心理';
    if (text === 'れ' && meaning === '受身') return '助動詞「る」連用形「れ」: 受身 (〜される)。「押しやられて」「知られぬる」「押し折られて」など、御息所側が主体性を奪われる文脈で頻用';
    if (text === 'れ' && meaning === '存続') return '助動詞「り」未然形「れ」: 存続 (〜ている)。「まじれれば」=混じっているので';
    if (text === 'べき') return '助動詞「べし」連体形「べき」: 推量・当然・意志・可能・命令・適当。終止形接続。ここは適当 (〜してよいような)。「さし退けなどすべき御車にもあらず」=立ち退かせてよい車ではない';
    if (text === 'む' && meaning === '意志') return '助動詞「む」: 意志 (〜よう)。「帰らむ」=帰ろう、と退出を試みる御息所の意思';
    if (text === 'む' && meaning === '婉曲') return '助動詞「む」: 婉曲 (〜ような)。連体修飾の位置では婉曲・仮定。「用意せむ」=配慮するような (こと)';
    if (text === 'む') return '助動詞「む」: 推量・意志・婉曲。未然形接続。文末は推量・意志、連体修飾位置では婉曲・仮定';
    if (text === 'らむ') return '助動詞「らむ」: 現在推量 (今ごろ〜だろう) ／ 現在原因推量。終止形接続。「思ひきこゆらむ」=思い申し上げているのだろう (侮蔑の含み)';
    if (text === 'まし') return '助動詞「まし」: 反実仮想 (もし〜だったら…だろうに)。「ましかば〜まし」が定型';
    if (text === 'ましか') return '助動詞「まし」未然形「ましか」: 反実仮想。「見ざらましかば」=もし見なかったとしたら。結びは省略され「口惜しからまし」などを補う';
    if (text === 'ざら') return '助動詞「ず」未然形「ざら」: 打消。「ざらまし」「ざらましかば」の形で反実仮想と組む';
    if (text === 'させ' && meaning === '使役') return '助動詞「さす」連用形「させ」: 使役 (〜させる)。「手触れさせず」=手を触れさせない、御息所方が葵の上方の供人を排除';
    if (text === 'せ' && meaning === '使役') return '助動詞「す」連用形「せ」: 使役 (〜させる)。「言はせそ」=言わせるな';
    if (text === 'さする') return '助動詞「さす」連体形「さする」: 使役 (〜させる)。「皆さし退けさする」=（供人たちに）皆立ち退かせる、葵の上方の権力が末端の暴力として現れる';
    return '助動詞「' + text + '」 (' + (meaning || form) + '): ' + (meaning ? '意味=' + meaning : '') + '。文中の働きと接続を確認';
  }

  // ===== 係助詞 =====
  if (pos === '係助詞') {
    if (text === 'ぞ') return '係助詞「ぞ」: 強意。結びは連体形 (係り結び)。「大将殿をぞ〜思ひきこゆらむ」では侮蔑のニュアンスを込めた強意';
    if (text === 'なむ') return '係助詞「なむ」: 強意 (〜こそ)。結びは連体形。「なむ」終助詞 (願望) と識別注意';
    if (text === 'や' && meaning === '疑問') return '係助詞「や」: 疑問・反語。結びは連体形。「もやと」「ばにや」など結び省略の余情用法に注意';
    if (text === 'や') return '係助詞「や」: 疑問・反語。結びは連体形 (係り結び)';
    if (text === 'か') return '係助詞「か」: 疑問・反語。結びは連体形 (係り結び)';
    if (text === 'こそ') return '係助詞「こそ」: 強意。結びは已然形 (係り結び)';
    if (text === 'は' && meaning === '提示') return '係助詞「は」: 提示・主題。「これは…御車にもあらず」「豪家には」のように対比・限定の含みを伴う';
    if (text === 'は') return '係助詞「は」: 主題・対比の提示。格助詞を抑えて主題を浮かせる';
    if (text === 'も' && meaning === '強意') return '係助詞「も」: 強意 (〜も)。「隙もなう」「ものも見えず」など、否定・強調の文脈で頻用';
    if (text === 'も' && meaning === '類似') return '係助詞「も」: 類似・並列 (〜もまた)。「その御方の人もまじれれば」=源氏の人も（葵の上方に）混じっているので';
    if (text === 'も' && meaning === '並列') return '係助詞「も」: 並列 (〜も〜も)。複数の要素を並べる';
    if (text === 'も') return '係助詞「も」: 並列・添加・強意。文脈で見分ける';
    return '係助詞「' + text + '」: 結びの形 (連体／已然) を確認。係り結びは古文読解の基幹';
  }

  // ===== 副助詞 =====
  if (pos === '副助詞') {
    if (text === 'のみ') return '副助詞「のみ」: 限定 (〜だけ・〜ばかり)。歌「影をのみみたらし川の」では「姿を見るだけ（に留まる）」と御息所の疎外感を表す';
    if (text === 'だに') return '副助詞「だに」: 類推 (〜さえ) ／ 最小限 (せめて〜だけでも)。「笹の隈にだにあらねばにや」=笹の隈でさえないからであろうか';
    if (text === 'さへ') return '副助詞「さへ」: 添加 (〜までも)。すでにある事柄に加えて。「だに」(類推) と区別';
    if (text === 'など') return '副助詞「など」: 例示・婉曲・引用。「〜など (と)」の形で発話・思考を婉曲化する用法も';
    if (text === 'ばかり') return '副助詞「ばかり」: 限定 (〜だけ) ／ 程度 (〜ほど)。文脈で見分ける';
    if (text === 'まで') return '副助詞「まで」: 範囲・程度。「〜にまで及ぶ」のニュアンス';
    return '副助詞「' + text + '」: 限定・添加・類推など。意味の見分けが頻出';
  }

  // ===== 終助詞 =====
  if (pos === '終助詞') {
    if (text === 'そ' && meaning === '禁止') return '終助詞「そ」: 禁止 (〜するな)。「な〜そ」の呼応で柔らかい禁止。「さな言はせそ」=そんなことを言わせるな';
    if (text === 'な' && meaning === '禁止') return '終助詞「な」: 終止形＋「な」で禁止 (〜するな)。「な〜そ」の呼応では副詞「な」（前）＋終助詞「そ」（後）';
    return '終助詞「' + text + '」: 文末で禁止・願望・詠嘆など。文脈で意味を確認';
  }

  // ===== 間投助詞 =====
  if (pos === '間投助詞') {
    if (text === 'や') return '間投助詞「や」: 詠嘆 (〜よ・〜なあ)。「心弱しや」=心が弱いことよ、という語り手の短い論評';
    return '間投助詞「' + text + '」: 文中で詠嘆・呼びかけ。語り手の感慨が滲む';
  }

  // ===== 補助動詞 =====
  if (pos === '補助動詞') {
    if (base === 'たまふ' || text.startsWith('たま')) {
      return '尊敬の補助動詞「たまふ」: 〜なさる・お〜になる。動作主への敬意。ハ行四段。「出でたまへり」「過ぎたまふ」など、葵の上・源氏・御息所への敬意';
    }
    if (base === 'おはす' || text.startsWith('おはし') || text === 'おはす') {
      return '尊敬の補助動詞「おはす」: 〜ていらっしゃる。サ変。「あり」の尊敬';
    }
    if (base === 'きこゆ' || text.includes('きこゆ') || text.startsWith('きこえ')) {
      return '謙譲の補助動詞「きこゆ」: お〜申し上げる。動作の受け手への敬意。ヤ行下二段。「思ひきこゆ」=お思い申し上げる';
    }
    if (base === 'あり' || text === 'あり' || text === 'あら' || text === 'あれ' || text === 'ある') {
      return '補助動詞「あり」: 「〜にあり」「〜てあり」の形で状態・存続を示す。ラ変。断定「なり」の核';
    }
    return '補助動詞「' + (base || text) + '」: 本動詞ではなく状態・敬意などを補う働き';
  }

  // ===== 格助詞 =====
  if (pos === '格助詞') {
    if (text === 'が' && meaning === '同格') return '格助詞「が」(同格): 〜で・〜であって。後ろに同じ性質の体言が省略。「網代の少し馴れたるが、下簾のさまなどよしばめる」=「網代車で、少し使い込まれていて、下簾の様子も上品な車」';
    if (text === 'の' && meaning === '同格') return '格助詞「の」(同格): 〜で・〜であって。連体修飾と紛らわしいが後ろに同じ性質の体言が省略される。「網代の少し馴れたる」=網代車で少し使い込まれたもの';
    if (text === 'の' && meaning === '主格') return '格助詞「の」(主格): 〜が。連体修飾節の中で主語を示す';
    if (text === 'にて') return '格助詞「にて」: 場所・時・手段・状態。「わざとならぬさまにて」=ことさらめかしくない様子で';
    if (text === 'と' && meaning === '引用') return '格助詞「と」(引用): 〜と。発話・思考を引用する。会話文・心内語の閉じに必須';
    return null;
  }

  // ===== 接続助詞 =====
  if (pos === '接続助詞') {
    if (text === 'ば' && form === '已然形') return '接続助詞「ば」: 已然形＋ば=順接確定 (〜ので・〜ところ・〜と)。前接形で意味が変わるので識別必須';
    if (text === 'ば') return '接続助詞「ば」: 未然形＋ば=順接仮定 (もし〜ならば) ／ 已然形＋ば=順接確定 (〜ので・〜ところ・〜と)';
    if (text === 'ど' || text === 'ども') return '接続助詞「ど／ども」: 逆接確定 (〜けれども)。已然形接続。「言へど」=言うけれども、「したまへど」=なさるけれども';
    if (text === 'て') return '接続助詞「て」: 順接 (〜て・〜のうえで)。連用形接続。動作の継起・並列を示す';
    if (text === 'で') return '接続助詞「で」: 打消接続 (〜ないで)。未然形接続。「ものも見で」=何も見ないで';
    if (text === 'つつ') return '接続助詞「つつ」: 反復・継続・同時並行 (〜ながら)。連用形接続。「うちかしこまり心ばへありつつ渡る」=かしこまり気を配りつつ通り過ぎる';
    if (text === 'ながら') return '接続助詞「ながら」: 並行 (〜ながら) ／ 逆接 (〜のに)。「いとほしと見ながら」=気の毒だと見ながらも';
    if (text === 'を' || text === 'に') return '接続助詞「を／に」: 順接 (〜ので) ／ 逆接 (〜のに) ／ 単純接続 (〜と)。連体形接続';
    if (text === 'が') return '接続助詞「が」: 逆接 (〜のに・〜が)。連体形接続';
    return null;
  }

  // ===== 動詞 =====
  if (pos === '動詞') {
    if (text === '思さ' || text === '思す' || base === '思す' || text === '思し乱るる') {
      return '尊敬動詞「思す」: お思いになる。「思ふ」の尊敬。サ四。御息所・葵の上・源氏など貴人の心情に用いる定型';
    }
    if (text === 'おはす' || text === 'おはせ' || base === 'おはす') {
      return '尊敬動詞「おはす」: いらっしゃる。「あり／行く／来」の尊敬。サ変';
    }
    if (text === '出で' || text === '出でて' || text === '出でたる') {
      return 'ダ行下二段「出づ」: 出る・現れる・出発する。「出でたまへり」=お出かけになっている、葵の上の華やかな出立';
    }
    if (text === '見ゆる' || text === '見ゆ') {
      return 'ヤ行下二段「見ゆ」: 見える・思われる・現れる。「しるく見ゆる」=はっきり見える、お忍びがかえって露見する逆説';
    }
    if (text === '思ひ' || text === '思ふ') {
      return '動詞「思ふ」: 思う・考える・感じる。古文最頻出動詞の一つ。尊敬「思す」と対比';
    }
    if (text === '言は' || text === '言ふ' || text === '言へ') {
      return '動詞「言ふ」: 言う・話す。「言はす」=言わせる、「言ふを」=言うのを';
    }
    if (text === '見') {
      return '動詞「見る」: 見る。マ行上一段。「見ず」「見で」など否定・接続の形に注意';
    }
    if (text === '見る') {
      return '動詞「見る」: マ行上一段。「見れ／見／見る／見る／見れ／見よ」と活用。連体形は「見る」';
    }
    if (text === '来') {
      return 'カ変動詞「来（く）」: こ／き／く／くる／くれ／こ（よ）。唯一「来」一語のみ。「来つらむ」=来てしまったのだろう';
    }
    if (text === '帰ら') {
      return 'ラ行四段「帰る」: 帰る。「帰らむとしたまへど」=帰ろうとなさるけれども、退出の試みが阻まれる';
    }
    if (text === 'まじれ') {
      return 'ラ行四段「まじる」: 混じる・入り込む。「その御方の人もまじれれば」=源氏の従者も（両陣営に）混じっているので';
    }
    if (text === '渡り' || text === '渡る') {
      return '動詞「渡る」: 通り過ぎる・通る。「御前渡り」「渡りたまふ」など、行列・通行の文脈で頻用';
    }
    if (text === 'つけ' || base === 'つく') {
      return '動詞「つく」: つく・接する・関連する。「につけても」=〜につけても、関連性を示す慣用句';
    }
    if (text === 'なり' && pos === '動詞') {
      return '動詞「なる」: ラ行四段。動作・変化「〜になる」。「事なりぬ」=（行列が）始まった';
    }
    if (text === 'こぼるる') {
      return 'ラ行下二段「こぼる」: あふれ出る・落ちる。「涙のこぼるる」=涙がこぼれる、御息所の慟哭';
    }
    if (text === 'とどめ') {
      return 'マ行下二段「とどむ」: 止める・心に留める。「後目にとどめたまふ」=横目で見て心に留める、源氏のさりげない視線';
    }
    if (text === 'ほほ笑み') {
      return '動詞「ほほ笑む」: 微笑する。源氏が他の物見車にだけ送る笑み。御息所への素通りと対比される';
    }
    if (text === '触れ') {
      return 'ラ行下二段「触る」: 触れる・関わる。「手触れさせず」=手を触れさせない';
    }
    if (text === 'かしこまり') {
      return 'ラ行四段「かしこまる」: 恐れ入る・かしこまる・うやうやしく振る舞う。源氏の供人が葵の上方に対して見せる礼儀正しさ';
    }
    if (text === '馴れ') {
      return 'ラ行下二段「馴る」: 慣れる・使い古す。「少し馴れたる」=少し使い込まれた網代車';
    }
    if (text === 'やつれ') {
      return 'ラ行下二段「やつる」: お忍びの粗末な姿をする・目立たないようにする。御息所のお忍びの作法';
    }
    if (text === '忍び') {
      return 'バ行上二段「忍ぶ」: こっそりする・隠れて〜する。「忍びて出でたまへる」=こっそり出かけていらっしゃった';
    }
    if (text === 'よしばめ') {
      return 'マ行四段「よしばむ」: 趣向を凝らして上品ぶる。「下簾のさまなどよしばめる」=下簾の様子も風情を凝らした';
    }
    if (text === 'かかる') {
      return '連体詞「かかる」: このような・こんな。「かかるやつれ」=このようなお忍びの姿';
    }
    if (ctype === 'サ変') return 'サ変動詞「' + (base || text) + '」: せ／し／す／する／すれ／せよ。「す・おはす」など';
    if (ctype === 'カ変') return 'カ変動詞「来 (く)」: こ／き／く／くる／くれ／こ (よ)。唯一「来」一語のみ';
    if (ctype === 'ナ変') return 'ナ変動詞「' + (base || text) + '」: な／に／ぬ／ぬる／ぬれ／ね。「死ぬ・往ぬ」の二語のみ';
    return null;
  }

  // ===== ダ下二・ヤ下二（pos表記が変則）=====
  if (pos === 'ダ下二') {
    return 'ダ行下二段「' + (base || text) + '」: 「出づ」など。「で／で／づ／づる／づれ／でよ」と活用';
  }
  if (pos === 'ヤ下二') {
    return 'ヤ行下二段「' + (base || text) + '」: 「見ゆ・覚ゆ・聞こゆ」など。「え／え／ゆ／ゆる／ゆれ／えよ」と活用';
  }

  // ===== 形容詞 =====
  if (pos === '形容詞') {
    if (text === '限りなし' || text.startsWith('限りな')) return '重要語「限りなし」: この上ない・最高だ。最上級表現。「いみじうねたきこと限りなし」=ひどく悔しいこと限りない、御息所の屈辱の頂点';
    if (text === 'うき' || text.startsWith('うき')) return '重要語「うし（憂し）」: つらい・嫌だ・憂鬱だ。「身のうき」=我が身のつらさ。歌では「浮き」(川の縁語) と「憂き」の掛詞';
    if (text === 'つらき' || text.startsWith('つら')) return '重要語「つらし」: 薄情だ・冷淡だ・恨めしい。「つらき人」=自分につらい思いをさせる人 (源氏)。現代語「辛い」より「相手への恨み」の含み';
    if (text === '心弱し' || text.startsWith('心弱')) return '重要語「心弱し」: 心が弱い・未練がましい。語り手が御息所の自発的な期待を「心弱しや」と論評する';
    return null;
  }

  // ===== 副詞 =====
  if (pos === '副詞') {
    if (text === 'いと') return '副詞「いと」: 非常に・たいそう。古文最頻出の程度副詞。打消を伴うと「あまり〜ない」(部分否定)';
    if (text === 'いとど') return '重要副詞「いとど」: いっそう・ますます。「いと」の強調形';
    return null;
  }

  // ===== 代名詞 =====
  if (pos === '代名詞') {
    if (text === 'これ') return '代名詞「これ」: 近称。「これは…御車にもあらず」=この車は…車ではない、と御息所の車を指して反論';
    if (text === 'それ') return '代名詞「それ」: 中称。「それと知られぬる」=（御息所だと）見抜かれてしまった';
    return null;
  }

  return null;
}

let added = 0;
let perCategory = { 助動詞: 0, 係助詞: 0, 副助詞: 0, 終助詞: 0, 間投助詞: 0, 補助動詞: 0, 動詞: 0, 形容詞: 0, 形容動詞: 0, 副詞: 0, 代名詞: 0, 格助詞: 0, 接続助詞: 0, 名詞: 0, ダ下二: 0, ヤ下二: 0 };
for (const s of data.sentences) {
  for (const tk of s.tokens) {
    if (tk.hint) continue;
    const h = buildHint(tk);
    if (h) {
      tk.hint = h;
      added++;
      const pos = (tk.grammarTag && tk.grammarTag.pos) || '名詞';
      const cat = pos in perCategory ? pos : '名詞';
      perCategory[cat]++;
    }
  }
}

fs.writeFileSync(SRC, JSON.stringify(data, null, 2), 'utf8');
console.log('Added hints:', added);
console.log('Per category:', perCategory);

const tokens = data.sentences.flatMap(s => s.tokens);
const meaningful = tokens.filter(tk => tk.layer > 0 || (tk.grammarTag && tk.grammarTag.pos));
const withHint = meaningful.filter(tk => tk.hint).length;
console.log('total tokens:', tokens.length);
console.log('meaningful:', meaningful.length);
console.log('with hint:', withHint);
console.log('coverage:', Math.round(withHint / meaningful.length * 100) + '%');

let ok = true;
for (const s of data.sentences) {
  const concat = s.tokens.map(tk => tk.text).join('');
  if (concat !== s.originalText) {
    ok = false;
    console.log('MISMATCH', s.id);
  }
}
console.log('alignment ok:', ok);
